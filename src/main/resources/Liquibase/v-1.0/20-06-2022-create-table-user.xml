<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
		https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.9.xsd">

    <changeSet id="11" author="Roman_Yaskevich">
        <comment>create table user</comment>
        <sql>
            create table user
            (
                id               int auto_increment not null,
                username         varchar(255) null,
                password         varchar(255) null,
                user_name        varchar(255) null,
                user_surname     varchar(255) null,
                email            varchar(255) null,
                phone_number     varchar(255) null,
                active           bit(1) not null,
                user_description varchar(255) null,
                constraint user_pk
                    primary key (id)
            )
        </sql>
    </changeSet>

    <changeSet id="12" author="Roman_Yaskevich">
        <comment>create table course_user</comment>
        <sql>
            create table course_user
            (
                id_user   int not null,
                id_course int not null,
                constraint user_course_fk
                    foreign key (id_user) references user (id),
                constraint course_user_fk
                    foreign key (id_course) references course (id)
            )
        </sql>
    </changeSet>
</databaseChangeLog>